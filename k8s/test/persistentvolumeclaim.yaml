apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: laravel-pvc-test
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi # Armazenamento para persistir dados no ambiente de testes
